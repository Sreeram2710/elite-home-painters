<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Quote Submitted - EliteHomePainters</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="container mt-5 text-center">
    <div class="alert alert-success shadow-lg p-4 rounded">
      <h2 class="fw-bold mb-3">🎉 Thank You for Your Request!</h2>
      <p class="lead">We’ve received your quote request and our team will get back to you shortly.</p>

      <% if (quote) { %>
        <hr />
        <div class="mt-4 text-start">
          <h5><strong>Name:</strong> <%= quote.name || "N/A" %></h5>
          <h5><strong>Service:</strong> <%= quote.paintType || "N/A" %></h5>
          
          <h5><strong>Message:</strong> <%= quote.message?.trim() ? quote.message : "–" %></h5>

          <h5 class="mt-3 text-success">
            <strong>🧾 Estimated Price:</strong> $<%= quote.estimatedPrice?.toFixed(2) || "0.00" %>
          </h5>
          <p class="text-muted">This is an auto-generated estimate based on the area you provided.</p>
        </div>
      <% } %>

      <a href="/" class="btn btn-primary mt-4">Return to Home</a>
    </div>
  </div>

  <%- include('partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
